#:kivy 1.9.0



AddLocationForm:

<AddLocationForm>: # taking @BoxLayout out and addressing with py file instead
	orientation: "vertical"
	search_results: search_results_list
	search_input: search_box # Use id and connect to property from main.py
	BoxLayout:
		height: "40dp"
		size_hint_y: None
		TextInput:
			id: search_box 	# set id to be refenced elsewhere in kvlang file
			size_hint_x: 50
			multiline: False
			on_text_validate: root.search_location()
		Button:
			text: "Search"
			size_hint_x: 25
			on_press: root.search_location()	# event handler on press/click
		Button:
			text: "Current Location"
			size_hint_x: 25
	ListView:
		id: search_results_list
		item_strings: ["Palo Alto, MX", "Palo Alto, US"]
